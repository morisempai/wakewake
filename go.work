go 1.23.0

// One module per service (ADR-0008). Each service owns its dependencies as it owns its database:
// exclusively. A shared root module would let a bump in one service silently move another.
//
// `gateway` is absent on purpose — ADR-0006 makes it off-the-shelf (Kong/APISIX/Traefik) with
// routing config, not Go code, so it has nothing to build.
use (
	./services/availability
	./services/booking
	./services/catalog
	./services/notification
	./services/payment
)
